# Cursor Rules

This file intentionally defers to `AGENTS.md` as the single source of truth.
If anything here conflicts with `AGENTS.md`, follow `AGENTS.md`.

Read order and conventions live in `AGENTS.md`.
